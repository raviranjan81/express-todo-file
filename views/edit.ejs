<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="w-full min-h-screen   bg-black p-10">
        <a class="text-blue-500 mb-10 inline-block" href="/" >GO TO Home page {Edit page}</a>
        <div class="form ">
            <form action="/edit" method="post">
                <input type="hidden" name="path" value="<%= filename %>">
                <% 
               let fil=filename.replace('.txt', '')
                %>
                <input type="text" name="title" class="mt-2 w-full block px-2 outline-none bg-state-600 p-3" value= <%=  fil %> placeholder="title">
                <textarea name="dec"   placeholder="write your task details here"
                    class="border w-full mt-2 px-2 outline-none bg-state-600 p-3"> <%= filedata %> </textarea>
                <button class="btn px-5 py-2 bg-yellow-500 text-white">Update</button>
            </form>
        </div>
    </div>
</body>

</html>