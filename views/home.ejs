<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen   bg-black">
        <div class="form p-10">
            <h1 class="mb-5 text-white">Simple to do without DB</h1>
            <form action="/create" method="post">
                <input type="text" name="title" class="mt-2 w-full block px-2 outline-none bg-state-600 p-3" placeholder="title">
                <textarea name="dec" placeholder="write your task details here"
                    class="border w-full mt-2 px-2 outline-none bg-state-600 p-3"></textarea>
                <button class="btn px-5 py-2 bg-blue-600 text-white">Submit</button>
            </form>
        </div>
        <div
            class="task p-10 grid  2xl:grid-cols-4 xl:grid-cols-4 lg:grid-cols-3 md:grid-cols-2 sm:grid-cols-1   gap-10 ">
                <% if (files.length>0) {%>
                    <% files.forEach(function (item){%>
                        <div class="task-card w-72 px-3 py-4 border ho " style="text-transform: capitalize;">
                            <h1 class="text-2xl text-white "><%= item %></h1>
                            <div class="flex justify-between items-center mt-2 ">
                                <a href="/file/<%= item %>" class="text-blue text-sm text-slate-500">read more</a>
                              <div class="flex items-center gap-4">
                                <a href="/delete/<%= item %>"  class="text-blue text-sm text-slate-400">delete</a>
                                <a href="/edit/<%= item %>" class="text-blue text-sm text-slate-400">Edit</a>
                              </div>
                            </div>
                        </div>
                    <%})%>
                <%} else{%>
                    <h3 class="text-zinc-600">No Task Yet.</h3>
                <%}%>
        </div>
    </div>
</body>

</html>